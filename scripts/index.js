// Projeto Tis

/* mobPrototype = {
  objects: {
    [objname, x, y]
  },
  frames: {
    [objname, x, y]
  }
}  */

function drawMap(map, texture) {
  for(let mx in map) {
    for(let my in mx) {
      
    }
  }
}

function main(stage, display){
  stage.viewbox(360,360)

}

document.onload = () => {
  Stage(main)
  Stage(mobile)
}